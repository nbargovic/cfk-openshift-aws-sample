apiVersion: platform.confluent.io/v1beta1
kind: KafkaRestProxy
metadata:
  name: kafkarestproxy
  namespace: confluent-laptop
spec:
  replicas: 2
  image:
    application: bargovic/cp-kafka-rest-repo1:6.2.1
    init: confluentinc/confluent-init-container:2.0.3
  tls:
    autoGeneratedCerts: true
  dependencies:
    schemaRegistry:
      url: http://schemaregistry.confluent-laptop.svc.cluster.local:8081
      tls:
        enabled: true
    kafka:
      bootstrapEndpoint: kafka.confluent-laptop.svc.cluster.local:9071
      tls:
        enabled: true
  podTemplate:
    podSecurityContext: { } # Disable the custom pod security context, to use the default
